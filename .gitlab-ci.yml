# .gitlab-ci.yml

image: ruby:3.3

pages:
  stage: deploy
  script:
    - gem install jekyll
    - gem install jekyll-remote-theme
    - jekyll build -d public
  artifacts:
    paths:
      - public
  only:
    - main
